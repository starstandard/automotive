openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: A standardized interface for Automotive Retail ...
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
servers:
- url: //v1_0_0
security:
- industry-oauth:
  - standard:read
- member-api-key: []
- {}
tags:
- name: Money
  description: Operations related to Money
- name: CashFlowScenario
  description: Operations related to CashFlowScenario
- name: Identifier
  description: Operations related to Identifier
- name: AccountingPeriod
  description: Operations related to AccountingPeriod
- name: CashFlowLineItem
  description: Operations related to CashFlowLineItem
- name: CashFlowProjection
  description: Operations related to CashFlowProjection
- name: Default
  description: Operations related to Default
- name: CashFlowProjectionCriteria
  description: Operations related to CashFlowProjectionCriteria
paths:
  /cash-flow-projections:
    get:
      tags:
      - CashFlowProjection
      summary: Retrieve a list of CashFlowProjection entities.
      description: list CashFlowProjection By
      operationId: listCashFlowProjection
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of CashFlowProjection entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CashFlowProjection"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: true
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - CashFlowProjection
      summary: Create a new CashFlowProjection entity.
      description: Create CashFlowProjection
      operationId: createCashFlowProjection
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: CashFlowProjection data to create the new resou...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCreate"
        required: true
      responses:
        "201":
          description: CashFlowProjection created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /cash-flow-projections/KEY78E
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/capabilities{?state,role}>; rel=\"\
                  capabilities\""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
  /cash-flow-projections/{cashFlowProjectionKey}:
    get:
      tags:
      - CashFlowProjection
      summary: Retrieve a specific CashFlowProjection entity.
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}"
      operationId: getCashFlowProjectionById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested CashFlowProjection entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionResponse"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - CashFlowProjection
      summary: Replace a CashFlowProjection entity.
      description: replace CashFlowProjection
      operationId: replaceCashFlowProjection
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjection"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - CashFlowProjection
      summary: Delete a CashFlowProjection entity
      description: Marks the CashFlowProjection entity as inactive...
      operationId: deleteCashFlowProjectionEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - CashFlowProjection
      summary: Partially update a CashFlowProjection entity.
      description: Partially update a CashFlowProjection
      operationId: updatePartialCashFlowProjection
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjection"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the specific CashFlowProjection.
      required: true
      schema:
        type: string
        example: KEY787
  /cash-flow-projections/{cashFlowProjectionKey}/money:
    get:
      tags:
      - Money
      summary: Retrieve a list of Money entities scoped by cas...
      description: list Money By CashFlowProjectionKey
      operationId: listMoneyByCashFlowProjectionKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of Money entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Money"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: true
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /cash-flow-projections/{cashFlowProjectionKey}/money/{moneyKey}:
    get:
      tags:
      - Money
      summary: Retrieve a specific Money entity.
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}/money/{moneyKey}"
      operationId: getMoneyById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested Money entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Money"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: moneyKey
      in: path
      description: The unique identifier for the specific Money.
      required: true
      schema:
        type: string
        example: KEY787C
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-scenarios:
    get:
      tags:
      - CashFlowScenario
      summary: Retrieve a list of CashFlowScenario entities sc...
      description: list CashFlowScenario By CashFlowProjectionKey
      operationId: listCashFlowScenarioByCashFlowProjectionKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of CashFlowScenario entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CashFlowScenario"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: true
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - CashFlowScenario
      summary: Create a new CashFlowScenario entity.
      description: Create CashFlowScenario
      operationId: createCashFlowScenario
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: CashFlowScenario data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowScenarioCreate"
        required: true
      responses:
        "201":
          description: CashFlowScenario created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: "/cash-flow-projections{cashFlowProjectionKey}cash-flow-scenarios/KEY78Eev"
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/{cashFlowProjectionKey}/cash-flow-scenarios/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-scenarios/{cashFlowScenarioKey}:
    get:
      tags:
      - CashFlowScenario
      summary: Retrieve a specific CashFlowScenario entity.
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-scenarios/{cashFlowScenarioKey}"
      operationId: getCashFlowScenarioById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested CashFlowScenario entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - CashFlowScenario
      summary: Replace a CashFlowScenario entity.
      description: replace CashFlowScenario
      operationId: replaceCashFlowScenario
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowScenario"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - CashFlowScenario
      summary: Delete a CashFlowScenario entity
      description: Marks the CashFlowScenario entity as inactive o...
      operationId: deleteCashFlowScenarioEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - CashFlowScenario
      summary: Partially update a CashFlowScenario entity.
      description: Partially update a CashFlowScenario
      operationId: updatePartialCashFlowScenario
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/CashFlowScenarioUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowScenario"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: cashFlowScenarioKey
      in: path
      description: The unique identifier for the specific CashFlowScenario.
      required: true
      schema:
        type: string
        example: KEY787Cm
  /cash-flow-projections/{cashFlowProjectionKey}/identifiers:
    get:
      tags:
      - Identifier
      summary: Retrieve a list of Identifier entities scoped b...
      description: list Identifier By CashFlowProjectionKey
      operationId: listIdentifierByCashFlowProjectionKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of Identifier entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Identifier"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: true
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - Identifier
      summary: Create a new Identifier entity.
      description: Create Identifier
      operationId: createIdentifier
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Identifier data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentifierCreate"
        required: true
      responses:
        "201":
          description: Identifier created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: "/cash-flow-projections{cashFlowProjectionKey}identifiers/KEY78Eevz"
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/{cashFlowProjectionKey}/identifiers/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /cash-flow-projections/{cashFlowProjectionKey}/identifiers/{identifierKey}:
    get:
      tags:
      - Identifier
      summary: Retrieve a specific Identifier entity.
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}/identifiers/{identifierKey}"
      operationId: getIdentifierById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested Identifier entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - Identifier
      summary: Replace a Identifier entity.
      description: replace Identifier
      operationId: replaceIdentifier
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Identifier"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - Identifier
      summary: Delete a Identifier entity
      description: Marks the Identifier entity as inactive or remo...
      operationId: deleteIdentifierEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - Identifier
      summary: Partially update a Identifier entity.
      description: Partially update a Identifier
      operationId: updatePartialIdentifier
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/IdentifierUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Identifier"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: identifierKey
      in: path
      description: The unique identifier for the specific Identifier.
      required: true
      schema:
        type: string
        example: KEY787Cmq
  /cash-flow-projections/{cashFlowProjectionKey}/accounting-periods:
    get:
      tags:
      - AccountingPeriod
      summary: Retrieve a list of AccountingPeriod entities sc...
      description: list AccountingPeriod By CashFlowProjectionKey
      operationId: listAccountingPeriodByCashFlowProjectionKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of AccountingPeriod entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/AccountingPeriod"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: true
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - AccountingPeriod
      summary: Create a new AccountingPeriod entity.
      description: Create AccountingPeriod
      operationId: createAccountingPeriod
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: AccountingPeriod data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriodCreate"
        required: true
      responses:
        "201":
          description: AccountingPeriod created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: "/cash-flow-projections{cashFlowProjectionKey}accounting-periods/KEY78EevzG"
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/{cashFlowProjectionKey}/accounting-periods/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /cash-flow-projections/{cashFlowProjectionKey}/accounting-periods/{accountingPeriodKey}:
    get:
      tags:
      - AccountingPeriod
      summary: Retrieve a specific AccountingPeriod entity.
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}/accounting-periods/{accountingPeriodKey}"
      operationId: getAccountingPeriodById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested AccountingPeriod entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - AccountingPeriod
      summary: Replace a AccountingPeriod entity.
      description: replace AccountingPeriod
      operationId: replaceAccountingPeriod
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccountingPeriod"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - AccountingPeriod
      summary: Delete a AccountingPeriod entity
      description: Marks the AccountingPeriod entity as inactive o...
      operationId: deleteAccountingPeriodEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - AccountingPeriod
      summary: Partially update a AccountingPeriod entity.
      description: Partially update a AccountingPeriod
      operationId: updatePartialAccountingPeriod
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/AccountingPeriodUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AccountingPeriod"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: accountingPeriodKey
      in: path
      description: The unique identifier for the specific AccountingPeriod.
      required: true
      schema:
        type: string
        example: KEY787CmqO
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-line-items:
    get:
      tags:
      - CashFlowLineItem
      summary: Retrieve a list of CashFlowLineItem entities sc...
      description: list CashFlowLineItem By CashFlowProjectionKey
      operationId: listCashFlowLineItemByCashFlowProjectionKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of CashFlowLineItem entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CashFlowLineItem"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: true
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - CashFlowLineItem
      summary: Create a new CashFlowLineItem entity.
      description: Create CashFlowLineItem
      operationId: createCashFlowLineItem
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: CashFlowLineItem data to create the new resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowLineItemCreate"
        required: true
      responses:
        "201":
          description: CashFlowLineItem created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: "/cash-flow-projections{cashFlowProjectionKey}cash-flow-line-items/KEY78EevzGV"
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/{cashFlowProjectionKey}/cash-flow-line-items/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-line-items/{cashFlowLineItemKey}:
    get:
      tags:
      - CashFlowLineItem
      summary: Retrieve a specific CashFlowLineItem entity.
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-line-items/{cashFlowLineItemKey}"
      operationId: getCashFlowLineItemById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested CashFlowLineItem entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - CashFlowLineItem
      summary: Replace a CashFlowLineItem entity.
      description: replace CashFlowLineItem
      operationId: replaceCashFlowLineItem
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowLineItem"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - CashFlowLineItem
      summary: Delete a CashFlowLineItem entity
      description: Marks the CashFlowLineItem entity as inactive o...
      operationId: deleteCashFlowLineItemEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - CashFlowLineItem
      summary: Partially update a CashFlowLineItem entity.
      description: Partially update a CashFlowLineItem
      operationId: updatePartialCashFlowLineItem
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/CashFlowLineItemUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowLineItem"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: cashFlowLineItemKey
      in: path
      description: The unique identifier for the specific CashFlowLineItem.
      required: true
      schema:
        type: string
        example: KEY787CmqO9
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-projection-criteria:
    get:
      tags:
      - CashFlowProjectionCriteria
      summary: Retrieve a list of CashFlowProjectionCriteria e...
      description: list CashFlowProjectionCriteria By CashFlowProj...
      operationId: listCashFlowProjectionCriteriaByCashFlowProjectionKey
      parameters:
      - name: sort
        in: query
        description: Sort Order of Result
        schema:
          type: string
      - name: filter
        in: query
        description: Filter Result
        schema:
          type: string
      - name: limit
        in: query
        description: Maximum number of entities to return.
        schema:
          maximum: 100
          minimum: 1
          type: integer
          default: 50
      - name: cursor
        in: query
        description: starting point for next set of results
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: A list of CashFlowProjectionCriteria entities.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - data
                - has_more
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/CashFlowProjectionCriteria"
                  next_cursor:
                    type: string
                    description: Opaque cursor for the next page. Null if no more
                      results.
                  has_more:
                    type: boolean
                    description: Whether additional results exist beyond this page.
                    example: false
                  total_count:
                    type: integer
                    description: "Total entities matching the query (optional, may\
                      \ be expensive)."
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    post:
      tags:
      - CashFlowProjectionCriteria
      summary: Create a new CashFlowProjectionCriteria entity.
      description: Create CashFlowProjectionCriteria
      operationId: createCashFlowProjectionCriteria
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: CashFlowProjectionCriteria data to create the n...
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCriteriaCreate"
        required: true
      responses:
        "201":
          description: CashFlowProjectionCriteria created successfully.
          headers:
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: "/cash-flow-projections{cashFlowProjectionKey}cash-flow-projection-criteria/KEY78EevzGVM"
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/{cashFlowProjectionKey}/cash-flow-projection-criteria/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
  /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-projection-criteria/{cashFlowProjectionCriteriaKey}:
    get:
      tags:
      - CashFlowProjectionCriteria
      summary: Retrieve a specific CashFlowProjectionCriteria ...
      description: "Standard GET operation for /cash-flow-projections/{cashFlowProjectionKey}/cash-flow-projection-criteria/{cashFlowProjectionCriteriaKey}"
      operationId: getCashFlowProjectionCriteriaById
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: The requested CashFlowProjectionCriteria entity.
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
    put:
      tags:
      - CashFlowProjectionCriteria
      summary: Replace a CashFlowProjectionCriteria entity.
      description: replace CashFlowProjectionCriteria
      operationId: replaceCashFlowProjectionCriteria
      parameters:
      - name: If-Match
        in: header
        description: The ETag of the resource to be replaced.
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Full entity data to replace the existing resource.
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCriteria"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the replaced resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    delete:
      tags:
      - CashFlowProjectionCriteria
      summary: Delete a CashFlowProjectionCriteria entity
      description: Marks the CashFlowProjectionCriteria entity as ...
      operationId: deleteCashFlowProjectionCriteriaEntity
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "204":
          description: Entity successfully deleted (or deactivated).
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "500":
          $ref: "#/components/responses/ServerError"
    patch:
      tags:
      - CashFlowProjectionCriteria
      summary: Partially update a CashFlowProjectionCriteria e...
      description: Partially update a CashFlowProjectionCriteria
      operationId: updatePartialCashFlowProjectionCriteria
      parameters:
      - name: If-Match
        in: header
        description: ETag of the resource being updated. update=succ...
        required: true
        schema:
          type: string
      - $ref: "#/components/parameters/StandardsVersionHeader"
      requestBody:
        description: Included Fields to be updated will be modified.
        content:
          application/merge-patch+json:
            schema:
              $ref: "#/components/schemas/CashFlowProjectionCriteriaUpdate"
        required: true
      responses:
        "200":
          description: Entity updated successfully. Returns the update...
          headers:
            ETag:
              description: The new ETag of the updated resource.
              schema:
                type: string
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CashFlowProjectionCriteria"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
    parameters:
    - name: cashFlowProjectionKey
      in: path
      description: The unique identifier for the parent resource.
      required: true
      schema:
        type: string
    - name: cashFlowProjectionCriteriaKey
      in: path
      description: The unique identifier for the specific CashFlowProjectionCriteria.
      required: true
      schema:
        type: string
        example: KEY787CmqO9M
  /cash-flow-projections/health:
    get:
      tags:
      - Default
      summary: Health Check
      description: Public endpoint to verify service availability.
      operationId: getCashFlowProjectionHealth
      parameters:
      - $ref: "#/components/parameters/StandardsVersionHeader"
      responses:
        "200":
          description: Service is healthy
          headers:
            standards-version:
              $ref: "#/components/headers/StandardsVersionResponse"
          content:
            application/json:
              schema:
                required:
                - status
                - version
                type: object
                properties:
                  status:
                    type: string
                    example: UP
                    enum:
                    - UP
                    - DOWN
                    - MAINTENANCE
                  version:
                    type: string
                    description: The version of the ontology implemented by this service.
                    example: 1.0.0
                  timestamp:
                    type: string
                    description: Server time in ISO-8601 format.
                    format: date-time
                    example: 2025-12-21T23:00:00Z
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
      security:
      - {}
  /cash-flow-projections/batch:
    post:
      tags:
      - Default
      summary: Bulk create cash-flow-projections
      description: Bulk Create of CashFlowProjection
      operationId: createCashFlowProjectionBatch
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                items:
                  maxItems: 100
                  type: array
                  items:
                    $ref: "#/components/schemas/CashFlowProjectionCreate"
      responses:
        "200":
          description: Batch processing complete
          headers:
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/batch/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /cash-flow-projections/batch/8227162561773626472
          content:
            application/json:
              schema:
                type: object
                properties:
                  succeeded:
                    type: array
                    items:
                      $ref: "#/components/schemas/CashFlowProjection"
                  failed:
                    type: array
                    items:
                      type: object
                      properties:
                        index:
                          type: integer
                          format: int32
                        error:
                          $ref: "#/components/schemas/ErrorDetail"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
        "409":
          $ref: "#/components/responses/ConflictError"
  /cash-flow-projections/exports:
    post:
      tags:
      - Default
      summary: Initiate a bulk CashFlowProjection export
      description: Standard POST operation for /cash-flow-projections/exports
      operationId: initiateCashFlowProjectionExport
      parameters:
      - name: Idempotency-Key
        in: header
        description: Unique string to prevent duplicate export jobs for the same request
        required: false
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                format:
                  type: string
                  example: ' 6735ff51'
                  default: PARQUET
                  enum:
                  - XML
                  - JSON
                  - AVRO
                  - PARQUET
                  - CSV
                date_range:
                  type: object
                  properties:
                    start:
                      type: string
                      format: date
                      example: 2026-01-14
                    end:
                      type: string
                      format: date
                      example: 2026-01-14
                data_contract_key:
                  type: string
                  description: "The identifier of the specific data contract/projection\
                    \ (e.g., 'customer_contact_v1')"
                  example: contact_details_only
                compression:
                  type: string
                  example: GZIP
                  default: SNAPPY
                  enum:
                  - SNAPPY
                  - GZIP
                  - LZO
                destination:
                  type: object
                  properties:
                    retrieval_url:
                      type: string
                      description: The URL where the user intends to receive or pull
                        the data
                      format: uri
                  description: Configuration for file retrieval
                notification:
                  type: object
                  properties:
                    webhook_url:
                      type: string
                      format: uri
                      example: https://client-system.com/callbacks/exports
                    signature_key:
                      type: string
                      description: A secret key used to sign the webhook payload for
                        security
                  description: "Optional: Callback configuration for completion alerts"
                dry_run:
                  type: boolean
                  description: "If true, the system validates the request and returns\
                    \ potential violations or metadata (like estimated size) without\
                    \ starting the job."
                  example: false
                  default: false
              example: PARQUET
      responses:
        "202":
          description: Export job created/accepted
          headers:
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/exports/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /cash-flow-projections/exports/4557695263319009087
        "409":
          description: "Conflict: Job with this Idempotency-Key already exists"
        "422":
          description: Unprocessable Entity - Business Rule Violation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProblemDetail"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
      callbacks:
        onExportCompleted:
          '{$request.body#/notification/webhook_url}':
            post:
              description: Sent when the export job reaches a terminal state (COMPLETED
                or FAILED)
              requestBody:
                content:
                  application/json:
                    schema:
                      $ref: "#/components/schemas/ExportNotification"
              responses:
                "200":
                  description: Webhook received
  /cash-flow-projections/exports/{exportId}:
    get:
      tags:
      - Default
      summary: Get export status
      description: Get Export Status of CashFlowProjection
      operationId: getCashFlowProjectionExportStatus
      parameters:
      - name: exportId
        in: path
        required: true
        schema:
          type: string
      responses:
        "200":
          description: Current status and file URLs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExportJob"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
  /cash-flow-projections/{cashFlowProjectionKey}/commands:
    post:
      tags:
      - Default
      summary: Execute a business workflow on CashFlowProjection
      description: Single entry point for all CashFlowProjection state-changing intents.
      operationId: executeCashFlowProjectionCommand
      parameters:
      - name: cashFlowProjectionKey
        in: path
        required: true
        schema:
          type: string
      requestBody:
        description: Command intent and parameters
        content:
          application/json:
            schema:
              required:
              - type
              - method
              - params
              - id
              type: object
              properties:
                type:
                  type: string
                  description: jsonrpc
                  default: jsonrpc
                jsonrpc:
                  type: string
                  example: "2.0"
                  default: "2.0"
                method:
                  type: string
                  description: star.order.update)
                id:
                  type: string
                  description: "match the response with request. If omitted, Notification\
                    \ the server will not respond."
                version:
                  type: string
                  description: "2.0"
                workflow_state:
                  type: string
                  description: "The intent of the command (e.g., CREATED, DELETED)"
                  enum:
                  - CREATED
                  - DELETED
                context:
                  type: object
                  additionalProperties: true
                  description: context payload required for this specific command
                    type
                params:
                  $ref: "#/components/schemas/CashFlowProjection"
      responses:
        "200":
          description: Commands processing complete
          headers:
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/{cashFlowProjectionKey}/commands/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: "/cash-flow-projections/{cashFlowProjectionKey}/commands/3184972921490546293"
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: The execution status of the command
                    example: SUCCESS
                    enum:
                    - SUCCESS
                    - FAILED
                    - PENDING
                    - PROCESSING
                    - VALIDATION_ERROR
                    - PARTIAL_SUCCESS
                    - REQUIRES_ACTION
                    - MITIGATION_APPLIED
                  message:
                    type: string
                    description: A human-readable summary or error detail
                    example: "Invoice #f9ff46dd has been successfully cancelled."
                  command_id:
                    type: string
                    description: The tracking ID for this specific command execution
                    format: identifier
                  code:
                    type: string
                    description: detailed code for workflow business processing
                    example: f5b8317c
                  retry_after:
                    type: string
                    description: "For PENDING or PROCESSING statuses, retry_after\
                      \ (integer, seconds) "
                  retryable:
                    type: boolean
                    description: "If true, the agent can attempt this command again\
                      \ immediately"
                    example: false
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
  /cash-flow-projections/{cashFlowProjectionKey}/commands/{commandId}:
    get:
      tags:
      - Default
      summary: Poll CashFlowProjection command status
      description: Check the progress or result of a previously submitted command.
      operationId: getCashFlowProjectionCommandStatus
      parameters:
      - name: cashFlowProjectionKey
        in: path
        required: true
        schema:
          type: string
          format: string
      - name: commandId
        in: path
        required: true
        schema:
          type: string
          format: string
      responses:
        "200":
          description: Current status of the command
          content:
            application/json:
              schema:
                type: object
                properties:
                  command_id:
                    type: string
                    description: The unique identifier for the command being tracked
                    format: uuid
                    readOnly: true
                  status:
                    type: string
                    description: The current lifecycle state of the command
                    enum:
                    - PENDING
                    - PROCESSING
                    - SUCCESS
                    - FAILED
                    - REQUIRES_ACTION
                  progress:
                    maximum: 100
                    minimum: 0
                    type: integer
                    description: Percentage of completion (0-100)
                    format: int32
                  message:
                    type: string
                    description: Status update or error message
                  result:
                    type: object
                    additionalProperties: true
                    description: The final output of the command (only populated on
                      SUCCESS)
                  errors:
                    type: array
                    items:
                      $ref: "#/components/schemas/ErrorDetail"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
  /cash-flow-projections/capabilities:
    get:
      tags:
      - Default
      summary: Discover CashFlowProjection Capabilities
      description: Standard GET operation for /cash-flow-projections/capabilities
      operationId: getCashFlowProjectionCapabilities
      parameters:
      - name: include
        in: query
        description: "Comma-separated list of sections to include (e.g., 'commands,exports,constraints')"
        schema:
          type: array
          items:
            type: string
            enum:
            - COMMANDS
            - EXPORTS
            - CONSTRAINTS
      - name: role
        in: query
        description: Filter capabilities by the agent's current role(s)
        schema:
          type: string
          example: ACCOUNTANT
      - name: state
        in: query
        description: Filter commands and constraints relevant only to a specific aggregate
          state
        required: true
        schema:
          type: string
          example: ACTIVE
      responses:
        "200":
          description: Successful discovery response (may be partial based on filters)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Capabilities"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "500":
          $ref: "#/components/responses/ServerError"
  /cash-flow-projections/documentation:
    post:
      tags:
      - Default
      summary: Execute a knowledge request on CashFlowProjection
      description: Single entry point for all CashFlowProjection knowledge request
      operationId: getCashFlowProjectionDocuments
      parameters:
      - name: cashFlowProjectionKey
        in: path
        required: true
        schema:
          type: string
      requestBody:
        description: Command intent and parameters
        content:
          application/json:
            schema:
              type: object
              properties:
                type:
                  type: string
                  description: "The intent of the document request (e.g., RescheduleSlot,\
                    \ CancelInvoice)"
                workflow_state:
                  type: string
                  description: "The intent of the command (e.g., PROCESS, CLASS)"
                  enum:
                  - PROCESS
                  - CLASS
                context:
                  type: object
                  additionalProperties: true
                  description: The payload required for this specific command type
                data:
                  $ref: "#/components/schemas/CashFlowProjection"
      responses:
        "200":
          headers:
            Link:
              description: HATEOAS link to discover state-aware transitions and business
                constraints.
              schema:
                type: string
                example: "</cash-flow-projections/documentation/capabilities{?state,role}>;\
                  \ rel=\"capabilities\""
            Location:
              description: The URI of the newly created resource.
              schema:
                type: string
                example: /cash-flow-projections/documentation/8111671910912204901
          $ref: "#/components/responses/CashFlowProjectionDocumentation"
        "400":
          $ref: "#/components/responses/BadRequestError"
        "401":
          $ref: "#/components/responses/UnauthorizedError"
        "403":
          $ref: "#/components/responses/ForbiddenError"
        "404":
          $ref: "#/components/responses/NotFoundError"
        "409":
          $ref: "#/components/responses/ConflictError"
        "412":
          $ref: "#/components/responses/PreconditionFailedError"
        "500":
          $ref: "#/components/responses/ServerError"
components:
  schemas:
    AccountingPeriod:
      type: object
      properties:
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        period_desc:
          type: string
          description: Descriptive name for the accounting peri...
          example: ' e63f547d'
        accounting_period_id:
          type: string
          description: Business key for the accounting period (...
          readOnly: true
          example: ' a3a265ab'
        accounting_period_key:
          type: string
          description: Business key for the accounting period (...
          example: ' 2dcb4ff2'
        period_name:
          type: string
          description: Display name of the accounting period.
          example: ' 4adccd60'
        start_date:
          type: string
          description: The beginning date of the accounting per...
          format: date
          example: 2026-01-14
        end_date:
          type: string
          description: The ending date of the accounting period...
          format: date
          example: 2026-01-14
        accounting_period_status_type:
          $ref: "#/components/schemas/AccountingPeriodStatusTypes"
        fiscal_year:
          type: integer
          description: The fiscal year this period belongs to.
          format: int32
          example: 1
      description: The fiscal timeframe used for reporting and aud...
    AccountingPeriodCreate:
      required:
      - accounting_period_key
      type: object
      description: AccountingPeriod Create for Post
      allOf:
      - $ref: "#/components/schemas/AccountingPeriod"
    AccountingPeriodStatusTypes:
      type: string
      description: types of accounting period status.
      enum:
      - OPEN
      - ADJUSTMENT
      - CLOSED
      - FUTURE
      - PENDING_CLOSURE
      - LOCKED
      - OTHER
    AccountingPeriodUpdate:
      type: object
      properties:
        end_date:
          type: string
          description: The ending date of the accounting period...
          format: date
          example: 2026-01-14
        period_desc:
          type: string
          description: Descriptive name for the accounting peri...
          example: ' e63f547d'
        fiscal_year:
          type: integer
          description: The fiscal year this period belongs to.
          format: int32
          example: 1
        identifiers:
          $ref: "#/components/schemas/Identifiers"
        accounting_period_status_type:
          $ref: "#/components/schemas/AccountingPeriodStatusTypes"
        period_name:
          type: string
          description: Display name of the accounting period.
          example: ' 4adccd60'
        start_date:
          type: string
          description: The beginning date of the accounting per...
          format: date
          example: 2026-01-14
      description: AccountingPeriod Update for Patch and Put
    Capabilities:
      type: object
      properties:
        aggregate:
          type: string
          example: CashFlowProjection
        version:
          type: string
          example: 1.2.0
        current_state:
          type: string
          description: The state used to filter these capabilities
          example: PENDING_APPROVAL
        transitions:
          type: array
          items:
            type: object
            properties:
              action:
                type: string
                example: APPROVE_INVOICE
              from_states:
                type: array
                example:
                - PENDING_APPROVAL
                items:
                  type: string
              to_state:
                type: string
                example: ISSUED
              required_roles:
                type: array
                description: Roles authorized to trigger this transition
                example:
                - MANAGER
                - FINANCE_VP
                items:
                  type: string
              links:
                type: object
                properties:
                  execute:
                    type: string
                    description: The relative or absolute URL to POST the command
                      to
                    example: "/cash-flow-projections/{key}/commands/approve"
                  method:
                    type: string
                    example: POST
                    enum:
                    - POST
                    - PUT
                    - PATCH
                  schema_ref:
                    type: string
                    description: Reference to the input payload schema for this action
                    example: "#/components/schemas/CashFlowProjectionCommand"
                description: HATEOAS-style links to execute the transition or fetch
                  related metadata
              _self:
                type: string
                description: Link to the specific transition documentation or detail
                example: /metadata/capabilities/CashFlowProjection/transitions/approve
              instructions:
                type: array
                description: Step-by-step checklist the agent must verify before execution
                example:
                - Verify credit limit is sufficient
                - Confirm tax jurisdiction matches customer address
                - Ensure currency matches original purchase order
                items:
                  type: string
              parameters_ref:
                type: string
                example: "#/components/schemas/CashFlowProjection"
        available_exports:
          type: array
          items:
            type: object
            properties:
              data_contract_key:
                type: string
                example: customer_contact_v1
              description:
                type: string
                example: Optimized view containing only non-sensitive contact details.
              supported_formats:
                type: array
                example:
                - PARQUET
                - CSV
                items:
                  type: string
                  enum:
                  - XML
                  - JSON
                  - AVRO
                  - PARQUET
                  - CSV
                default: PARQUET
              columns:
                type: array
                example:
                - first_name
                - last_name
                - email
                items:
                  type: string
              retention_policy:
                type: object
                properties:
                  retention_period:
                    type: string
                    description: ISO-8601 duration the file remains available for
                      download
                    example: P7D
                  is_pii_sensitive:
                    type: boolean
                    description: Indicates export contains PII
                    example: false
                  access_tier:
                    type: string
                    description: The security clearance required for this contract
                    example: INTERNAL
                    enum:
                    - PUBLIC
                    - INTERNAL
                    - RESTRICTED
                    - CONFIDENTIAL
              constraints:
                type: array
                items:
                  type: object
                  properties:
                    rule_id:
                      type: string
                      description: Unique identifier for the business rule
                      readOnly: true
                      example: IDENTITY_LOCK_STATE
                    constraint_type:
                      type: string
                      enum:
                      - RANGE_LIMIT
                      - SIZE_LIMIT
                      - FORMAT_INCOMPATIBILITY
                      - PATTERN_MISMATCH
                      - PERMISSION_LOCK
                      - MANDATORY_MISSING
                      - STATE_TRANSITION_ERROR
                      - IMMUTABILITY_VIOLATION
                      - RELATIONAL_DEPENDENCY
                      - CONFLICT_DETECTION
                      - AGGREGATE_LIMIT
                      - ORPHAN_PREVENTION
                    target_fields:
                      type: array
                      description: The specific fields or attributes this constraint
                        applies to
                      example:
                      - first_name
                      - last_name
                      - ssn
                      items:
                        type: string
                    rule_context:
                      type: object
                      additionalProperties:
                        type: string
                      description: Key-value pairs representing the 'facts' of the
                        current state
                      example:
                        limit: "0"
                        item_type: Inventory
                        count: "45"
                    rule_constraints:
                      type: object
                      additionalProperties:
                        type: string
                      description: "Key-value pairs for simple UI-driven validation\
                        \ (e.g., min, max, unit). Use this for 'Fast Path' client-side\
                        \ checks."
                      example:
                        unit: days
                        min: "1"
                        value: "31"
                    limit_rule:
                      type: string
                      description: A Prolog-based predicate defining the valid boundary
                        for this constraint.
                      example: "allow_export(Format, Days) :- Format == 'parquet',\
                        \ Days =< 31; Format == 'csv', Days =< 90."
                    message:
                      type: string
                      description: Human-readable explanation for UI/Logs
                      example: Identity fields cannot be modified while in PENDING_APPROVAL
                        state.
                    severity:
                      type: string
                      enum:
                      - ERROR
                      - WARNING
                    link:
                      type: string
                      description: Link to documentation regarding this rule
                      format: uri
        documentation_ref:
          type: string
          description: Link to full business documentation for this aggregate
          format: uri
          example: /documentation/cash-flow-projections
    CashFlowLineItem:
      type: object
      properties:
        cash_flow_line_item_id:
          type: string
          description: The unique key for the Cash Flow LineIte...
          readOnly: true
          example: ' f1b0f327'
        cash_flow_line_item_key:
          type: string
          description: The unique key for the Cash Flow LineIte...
          example: ' eefe5a7e'
        period_start_date:
          type: string
          description: The start date of the reporting period f...
          format: date
          example: 2026-01-14
        period_end_date:
          type: string
          description: The end date of the reporting period for...
          format: date
          example: 2026-01-14
        cash_inflow_total:
          $ref: "#/components/schemas/Money"
        cash_outflow_total:
          $ref: "#/components/schemas/Money"
        net_cash_flow:
          $ref: "#/components/schemas/Money"
        cash_balance_closing:
          $ref: "#/components/schemas/Money"
        source_category:
          type: string
          description: The source type or category for this flo...
          example: ' 1a836a2e'
      description: A specific entry in a cash flow statement.
    CashFlowLineItemCreate:
      required:
      - cash_flow_line_item_key
      type: object
      description: CashFlowLineItem Create for Post
      allOf:
      - $ref: "#/components/schemas/CashFlowLineItem"
    CashFlowLineItemUpdate:
      type: object
      properties:
        net_cash_flow:
          $ref: "#/components/schemas/Money"
        period_end_date:
          type: string
          description: The end date of the reporting period for...
          format: date
          example: 2026-01-14
        cash_balance_closing:
          $ref: "#/components/schemas/Money"
        period_start_date:
          type: string
          description: The start date of the reporting period f...
          format: date
          example: 2026-01-14
        cash_outflow_total:
          $ref: "#/components/schemas/Money"
        source_category:
          type: string
          description: The source type or category for this flo...
          example: ' 1a836a2e'
        cash_inflow_total:
          $ref: "#/components/schemas/Money"
      description: CashFlowLineItem Update for Patch and Put
    CashFlowLineItems:
      type: array
      items:
        $ref: "#/components/schemas/CashFlowLineItem"
    CashFlowProjection:
      type: object
      properties:
        cash_flow_projection_key:
          type: string
          description: The unique key for the Cash Flow Project...
          example: ' dc22b171'
        cash_flow_projection_id:
          type: string
          description: The unique key for the Cash Flow Project...
          readOnly: true
          example: ' 33ec42a1'
        cash_flow_projection_criteria:
          $ref: "#/components/schemas/CashFlowProjectionCriteria"
        projection_date:
          type: string
          description: The date the projection was generated.
          format: date
          example: 2026-01-14
        cash_balance_opening:
          $ref: "#/components/schemas/Money"
        scenario:
          $ref: "#/components/schemas/CashFlowScenario"
        accounting_period:
          $ref: "#/components/schemas/AccountingPeriod"
        line_items:
          $ref: "#/components/schemas/CashFlowLineItems"
      description: Forecasted movement of cash into and out of the...
    CashFlowProjectionCreate:
      required:
      - cash_flow_projection_key
      type: object
      description: CashFlowProjection Create for Post
      allOf:
      - $ref: "#/components/schemas/CashFlowProjection"
    CashFlowProjectionCriteria:
      type: object
      properties:
        budget_variance_report_request_id:
          type: string
          description: The Accounting Period Key for which the ...
          readOnly: true
          example: ' dfa2f875'
        budget_variance_report_request_key:
          type: string
          description: The Accounting Period Key for which the ...
          example: ' fe9debdd'
        period_granularity_type:
          $ref: "#/components/schemas/UOMTimeTypes"
        scenario_key:
          type: string
          description: Optional key of a pre-defined CashFlowSc...
          example: ' 1e43b966'
        accounting_period_key:
          type: string
          description: The Accounting Period Key for which the ...
          example: ' 9594746f'
        gl_account_keys:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        control_account_keys:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        organization_id:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          readOnly: true
          items:
            type: string
        start_date:
          type: string
          description: The start date for the forecast period (...
          format: date
          example: 2026-01-14
        end_date:
          type: string
          description: The end date for the forecast period (in...
          format: date
          example: 2026-01-14
        opening_cash_balance:
          $ref: "#/components/schemas/Money"
      description: Parameters for calculating cash projections.
    CashFlowProjectionCriteriaCreate:
      required:
      - cash_flow_projection_criteria_key
      type: object
      description: CashFlowProjectionCriteria Create for Post
      allOf:
      - $ref: "#/components/schemas/CashFlowProjectionCriteria"
    CashFlowProjectionCriteriaUpdate:
      type: object
      properties:
        period_granularity_type:
          $ref: "#/components/schemas/UOMTimeTypes"
        end_date:
          type: string
          description: The end date for the forecast period (in...
          format: date
          example: 2026-01-14
        opening_cash_balance:
          $ref: "#/components/schemas/Money"
        gl_account_keys:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        control_account_keys:
          maxItems: 255
          minItems: 1
          uniqueItems: true
          type: array
          items:
            type: string
        start_date:
          type: string
          description: The start date for the forecast period (...
          format: date
          example: 2026-01-14
      description: CashFlowProjectionCriteria Update for Patch and...
    CashFlowProjectionResponse:
      allOf:
      - $ref: "#/components/schemas/CashFlowProjection"
      - $ref: "#/components/schemas/CashFlowProjectionWorkflowMetadata"
    CashFlowProjectionUpdate:
      type: object
      properties:
        scenario:
          $ref: "#/components/schemas/CashFlowScenario"
        cash_balance_opening:
          $ref: "#/components/schemas/Money"
        cash_flow_projection_criteria:
          $ref: "#/components/schemas/CashFlowProjectionCriteria"
        line_items:
          $ref: "#/components/schemas/CashFlowLineItems"
        accounting_period:
          $ref: "#/components/schemas/AccountingPeriod"
        projection_date:
          type: string
          description: The date the projection was generated.
          format: date
          example: 2026-01-14
      description: CashFlowProjection Update for Patch and Put
    CashFlowProjectionWorkflowMetadata:
      type: object
      properties:
        current_state:
          type: string
          description: The current lifecycle phase of this entity.
          example: ' 11d687a0'
          enum:
          - CREATED
          - ACTIVE
          - DELETED
          - ARCHIVED
        allowed_commands:
          type: array
          description: Intents the agent can execute on this specific instance right
            now.
          items:
            $ref: "#/components/schemas/CommandOption"
    CashFlowScenario:
      type: object
      properties:
        scenario_key:
          type: string
          description: The unique key for the Cash Flow Scenari...
          example: ' 79f756e0'
        name:
          type: string
          description: A user-friendly name for the scenario (e...
          example: ' 4dcc794b'
        ar_collection_delay_days:
          type: integer
          description: The average number of days past due date...
          format: int32
          example: 1
        ap_payment_hold_days:
          type: integer
          description: The average number of days the business ...
          format: int32
          example: 1
        description:
          type: string
          description: A description of the assumptions used in...
          example: ' 6255177a'
      description: not nullable
    CashFlowScenarioCreate:
      required:
      - cash_flow_scenario_key
      type: object
      description: CashFlowScenario Create for Post
      allOf:
      - $ref: "#/components/schemas/CashFlowScenario"
    CashFlowScenarioUpdate:
      type: object
      properties:
        ar_collection_delay_days:
          type: integer
          description: The average number of days past due date...
          format: int32
          example: 1
        name:
          type: string
          description: A user-friendly name for the scenario (e...
          example: ' 4dcc794b'
        description:
          type: string
          description: A description of the assumptions used in...
          example: ' 6255177a'
        ap_payment_hold_days:
          type: integer
          description: The average number of days the business ...
          format: int32
          example: 1
      description: CashFlowScenario Update for Patch and Put
    Codes:
      type: array
      items:
        type: string
    CommandOption:
      type: object
      properties:
        type:
          type: string
          description: The command name to send to the /commands endpoint
          example: Approve
        description:
          type: string
          description: Guidance for the agent on when to use this command
          example: Moves the invoice to ACTIVE state and triggers the payment gateway.
        target_state:
          type: string
          description: The state the entity will transition to upon success
          example: ACTIVE
    ErrorDetail:
      required:
      - category
      - code
      - message
      - timestamp
      - trace_id
      type: object
      properties:
        code:
          type: string
          description: "A machine-readable, unique system error code."
          example: NOT_FOUND_PR
        message:
          type: string
          description: A developer-friendly explanation of the error.
          example: Personnel Record with ID PR-1234567 was not found.
        category:
          type: string
          description: Classifies the error to help clients determine ...
          example: DATA_INTEGRITY
          enum:
          - VALIDATION
          - AUTHENTICATION
          - AUTHORIZATION
          - INFRASTRUCTURE
          - BUSINESS_LOGIC
          - DATA_INTEGRITY
        timestamp:
          type: string
          description: The time the error occurred (ISO 8601).
          format: date-time
          example: 2025-11-10T10:30:00Z
        trace_id:
          type: string
          description: A unique identifier for this specific request.
          readOnly: true
          example: 57c31110-859a-11ec-a8a3-0242ac120002
        doc_url:
          type: string
          description: A URL leading to the documentation page for thi...
          format: uri
          example: https://api.docs.com/errors/NOT_FOUND_PR
        details:
          type: array
          description: Field-level validation errors or granular diagn...
          items:
            type: object
            properties:
              field:
                type: string
                description: The JSON path or field name that caused the error.
                example: payrollId
              issue:
                type: string
                description: The specific validation rule that failed.
                example: must be unique
              rejected_value:
                type: string
                description: The value that was sent and rejected (only if non-sensitive).
                example: "12345"
      description: Standardized error response for the entire API ...
    ExportFile:
      type: object
      properties:
        sequence:
          type: integer
          description: Part number
          format: int32
          example: 1
        download_url:
          type: string
          format: uri
          example: https://api.example.com
        format:
          type: string
          example: ' 6735ff51'
          default: PARQUET
          enum:
          - XML
          - JSON
          - AVRO
          - PARQUET
          - CSV
        expires_at:
          type: string
          format: date-time
          example: 2026-01-14T12:00:00Z
        checksum_sha256:
          type: string
          example: ' 4062911f'
    ExportJob:
      type: object
      properties:
        id:
          type: string
          readOnly: true
          example: ' b212de26'
        status:
          type: string
          example: ' f758ad07'
          enum:
          - PENDING
          - PROCESSING
          - COMPLETED
          - FAILED
        progress_percentage:
          maximum: 100
          minimum: 0
          type: integer
          format: int32
          example: 1
        files:
          type: array
          items:
            $ref: "#/components/schemas/ExportFile"
    ExportNotification:
      type: object
      properties:
        event_type:
          type: string
          example: EXPORT_COMPLETED
          enum:
          - EXPORT_COMPLETED
          - EXPORT_FAILED
        occurred_at:
          type: string
          format: date-time
          example: 2026-01-14T12:00:00Z
        data:
          type: object
          properties:
            job_id:
              type: string
              readOnly: true
              example: 96d8e26f
            aggregate:
              type: string
              example: CashFlowProjection
            data_product_id:
              type: string
              readOnly: true
            status:
              type: string
              example: FAILED
              enum:
              - COMPLETED
              - FAILED
            error_message:
              type: string
              description: Present only if status is FAILED
            files:
              type: array
              items:
                $ref: "#/components/schemas/ExportFile"
    Identifier:
      type: object
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          type: string
          description: Unique identifier value
          readOnly: true
          example: ' a7e762f0'
        identifier_key:
          maxLength: 50
          minLength: 5
          type: string
          description: Business key for the identifier
          readOnly: true
          example: ' a4ed673f'
        context:
          type: string
          description: Context or domain of the identifier
          example: ' 5854abb2'
        value:
          type: string
          description: The monetary or hours-based value of the...
          example: ' 39c38b7d'
        detail:
          type: string
          description: The monetary or hours-based value of the...
          example: ' 4fd9c15e'
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
          readOnly: true
          example: ' 1a6a2623'
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: Unique identification key for the record.
    IdentifierCreate:
      required:
      - identifier_key
      type: object
      description: Identifier Create for Post
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
          readOnly: true
          example: ' 1a6a2623'
        context:
          type: string
          description: Context or domain of the identifier
          example: ' 5854abb2'
        detail:
          type: string
          description: The monetary or hours-based value of the...
          example: ' 4fd9c15e'
        value:
          type: string
          description: The monetary or hours-based value of the...
          example: ' 39c38b7d'
      description: Identifier Update for Patch and Put
    Identifiers:
      type: array
      items:
        $ref: "#/components/schemas/Identifier"
    Money:
      type: object
      properties:
        money_key:
          type: string
          description: The unique key for the monetary value.
          example: ' 21030e9a'
        money_id:
          type: string
          description: The unique ID for the monetary value.
          readOnly: true
          example: ' 928b7960'
        currency_code:
          type: string
          description: "ISO currency code, e.g., 'USD'."
          example: ' f79447b5'
        display_value:
          type: string
          description: The locale-specific display format for t...
          example: ' be5a4fe6'
        locale:
          type: string
          description: "The locale code (e.g., en-US, fr-FR)."
          example: ' 0a2c19eb'
        value:
          type: string
          description: The monetary value.
          example: ' ba261904'
      description: Monetary value and currency information.
    ProblemDetail:
      type: object
      properties:
        type:
          type: string
          format: uri
          example: https://api.yourdomain.com/errors/validation-failed
        title:
          type: string
          example: Intent Validation Failed
        status:
          type: integer
          format: int32
          example: 422
        detail:
          type: string
          example: The export request could not be processed due to business constraints.
        instance:
          type: string
          format: uri
          example: https://api.yourdomain.com/money/exports
        intent:
          type: string
          example: InitiateMoneyExport
        violations:
          type: array
          items:
            type: object
            properties:
              rule:
                type: string
                example: INSUFFICIENT_FUNDS
              message:
                type: string
                example: Export exceeds your monthly data tier limit.
              path:
                type: string
                example: date_range.start
              metadata:
                type: object
                additionalProperties:
                  type: string
    UOMTimeTypes:
      type: string
      description: types of u o m times.
      enum:
      - NANOSECOND
      - MICROSECOND
      - MILLISECOND
      - SECOND
      - MINUTE
      - HOUR
      - DAY
      - WEEK
      - MONTH
      - YEAR
      - DECADE
      - CENTURY
      - MILLENNIUM
      - PERCENTAGE_OF_HOUR
      - DURATION
      - MINUTES
      - HOURS
      - DAYS
      - WEEKS
      - DAY_OF_MONTH
      - BI_WEEKS
      - SEMI_MONTHS
      - MONTHS
      - QUARTERS
      - SEMI_YEARS
      - YEARS
      - MULTI_YEARS
      - OCCURRENCE
      - UNLIMITED
      - NONE
      - CUSTOM
      - OTHER
  responses:
    CashFlowProjectionDocumentation:
      description: Commands processing complete
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CashFlowProjectionDocumentation"
    ForbiddenError:
      description: The authenticated user does not have the necess...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ConflictError:
      description: The request could not be completed because it c...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    ServerError:
      description: An unexpected error occurred on the server whil...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    PreconditionFailedError:
      description: The request was not processed because the 'If-M...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    NotFoundError:
      description: The requested resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    BadRequestError:
      description: "The request was malformed, failed validation, o..."
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
    UnauthorizedError:
      description: Authentication failed. The access token is miss...
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorDetail"
  parameters:
    StandardsVersionHeader:
      name: standards-version
      in: header
      required: true
      schema:
        pattern: ^\d+(\.\d+)*$
        type: string
        example: "2026.1"
  headers:
    StandardsVersionResponse:
      description: Standards Version
      schema:
        pattern: ^\d+(\.\d+)*$
        type: string
        example: "2026.1"
  securitySchemes:
    industry-oauth:
      type: oauth2
      description: Centralized OAuth2 service for all industry members
      flows:
        clientCredentials:
          tokenUrl: https://auth.example.com/token
          scopes:
            standard:read: Read access
    member-api-key:
      type: apiKey
      name: X-API-KEY
      in: header
    bearer-auth:
      type: http
      description: JWT token obtained from authentication service
      scheme: bearer
      bearerFormat: JWT
