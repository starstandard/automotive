openapi: 3.0.1
info:
  title: Automotive Retail Systems API
  description: "A standardized interface for Automotive Retail operations, built upon\
    \ a formal Retail Ontology. It enables seamless integration \n\n**Key Capabilities:**\n\
    * **Catalog Management:** Unified definitions for parts, assemblies, and BOMs.\n\
    * **Inventory Orchestration:** Real-time visibility into warehouse and dealership\
    \ stock.\n* **Financial Workflows:** Automated invoicing and batch processing\
    \ for high-volume retail transactions.\n\nDesigned for high-reliability CI/CD\
    \ environments and asynchronous batch processing."
  contact:
    name: Platform Architecture Team
    url: www.starstandard.org
    email: pescobar@starstandard.org
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  version: 1.0.0
paths: {}
components:
  schemas:
    Codes:
      type: array
      items:
        type: string
    EventTypes:
      type: string
      description: types of events.
      enum:
      - INVOICE_RECEIVED
      - ITEM_SHIPPED
    Identifier:
      type: object
      properties:
        identifier_id:
          maxLength: 50
          minLength: 5
          type: string
          description: Unique identifier value
        identifier_key:
          maxLength: 50
          minLength: 5
          type: string
          description: Business key for the identifier
        context:
          type: string
          description: Context or domain of the identifier
        value:
          type: string
          description: The monetary or hours-based value of the...
        detail:
          type: string
          description: The monetary or hours-based value of the...
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        system_codes:
          $ref: "#/components/schemas/Codes"
      description: Unique identification key for the record.
    IdentifierCreate:
      required:
      - identifier_key
      type: object
      description: Identifier Create for Post
      allOf:
      - $ref: "#/components/schemas/Identifier"
    IdentifierUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier_type:
          type: string
          description: The monetary or hours-based value of the...
        context:
          type: string
          description: Context or domain of the identifier
        detail:
          type: string
          description: The monetary or hours-based value of the...
        value:
          type: string
          description: The monetary or hours-based value of the...
      description: Identifier Update for Patch and Put
    NotificationReference:
      type: object
      properties:
        notification_reference_id:
          type: string
          description: Unique identifier value for the referenc...
        notification_reference_key:
          type: string
          description: Unique key value for the reference recor...
        source_key:
          maxLength: 50
          minLength: 5
          type: string
          description: The unique key identifying the reference...
        source_actor:
          maxLength: 50
          minLength: 5
          type: string
          description: The unique key identifying the reference...
        context:
          type: string
          description: Context or domain of the identifier (e.g...
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier:
          $ref: "#/components/schemas/Identifier"
        occurrence_date_time:
          type: string
          description: The date and time the reference was crea...
          format: date-time
        event_type:
          $ref: "#/components/schemas/EventTypes"
      description: Link to the associated system notification.
    NotificationReferenceCreate:
      required:
      - notification_reference_key
      type: object
      description: NotificationReference Create for Post
      allOf:
      - $ref: "#/components/schemas/NotificationReference"
    NotificationReferenceUpdate:
      type: object
      properties:
        system_codes:
          $ref: "#/components/schemas/Codes"
        identifier:
          $ref: "#/components/schemas/Identifier"
        event_type:
          $ref: "#/components/schemas/EventTypes"
        context:
          type: string
          description: Context or domain of the identifier (e.g...
        source_actor:
          maxLength: 50
          minLength: 5
          type: string
          description: The unique key identifying the reference...
        occurrence_date_time:
          type: string
          description: The date and time the reference was crea...
          format: date-time
      description: NotificationReference Update for Patch and Put
